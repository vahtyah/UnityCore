namespace StandardAssets
{
    /// <summary>
    /// Nhóm UI cho module Item (dùng với SAUIGroupManager). Tag [SAEnum("UIGroup")]
    /// để xuất hiện trong các bộ lọc enum của hệ thống SA.
    /// </summary>
    [SAEnum("UIGroup")]
    public enum UIItem
    {
        ItemDisplay
    }
}
